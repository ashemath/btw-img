# Parameters for creating the spare disk image for builder
BUILD=debian
BUILDNAME=debian
BUILDSIZE=20G

# Script to run inside the chroot environment after basic setup
BUILDPOSTINIT="./conf.d/docker.sh"
LAYOUT="efi-basic"

# Admin user to setup initial admin access on built images
ADMIN_USER="btw"

# Not good practice to put a password here, so I'll leave this comment here..
# ADMIN_PASSWD=
